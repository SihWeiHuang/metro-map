/** Public site links — set in Vercel / local `.env` (optional). */
export const SITE_CONTACT_EMAIL =
  import.meta.env.VITE_SITE_CONTACT_EMAIL?.trim() || "";

export const SPONSOR_URL = import.meta.env.VITE_SPONSOR_URL?.trim() || "";

export const SITE_LAST_UPDATED = "2026-06-05";
